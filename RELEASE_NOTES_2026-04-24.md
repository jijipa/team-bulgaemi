# Release Notes - 2026-04-24

## 요약

이번 업데이트에서는 메인 화면의 매치 카드 경험을 크게 다듬고, 용병 선택 모달 레이아웃을 Figma 기준에 맞춰 정리했습니다. 또한 기본 매치 배경 이미지를 교체하고, 배포까지 완료했습니다.

## 주요 변경사항

### 1. 메인 매치 카드 개선

- 스코어 미등록 매치 전용 카드 추가
- 매치 등록 후 아직 스코어를 입력하지 않은 경우, 메인 매치 섹션에 경기 정보 카드가 노출되도록 반영
- 표시 정보:
  - 종목
  - 인원 수
  - 날짜 / 요일 / 시작 시간
  - 장소
  - 경기 시간 / 쿼터 수
  - 상대팀 이름

### 2. 예정 경기 카드 UI를 Figma 기준으로 정리

- Figma 시안 기준으로 배지, 날짜, 장소, 경기시간, 상대팀 영역 레이아웃 수정
- 장소 링크가 있을 경우 복사 버튼 노출
- 복사 시 `주소가 복사되었습니다.` 토스트 노출
- 장소 텍스트는 기본적으로 hug 형태로 붙고, 길어질 때만 1줄 말줄임 처리

### 3. 결과 카드 레이아웃 보정

- 메인 화면 결과 카드의 점수 영역, 득점자 리스트 간격, 골 아이콘 영역 수치 조정
- 점수 자릿수에 따라 레이아웃 분기 적용
  - 한 자리 점수: 기본 수치 사용
  - 두 자리 점수 포함: 숫자 크기와 줄높이를 별도 수치로 조정
- 득점자 영역 2열 정렬 기준에 맞게 아이템 폭 및 간격 수정

### 4. 카드 이미지 로딩 경험 개선

- 메인 화면 결과 카드 이미지를 미리 프리로드
- 카드 이미지가 준비되기 전에는 스켈레톤을 먼저 보여주고, 준비 완료 후 카드가 한 번에 보이도록 개선
- 새로고침 시 카드 배경이 늦게 바뀌며 끊겨 보이던 현상 완화

### 5. 기본 매치 배경 이미지 교체

- 매치 이미지가 없는 경우 표시되는 기본 배경 이미지를 새로운 녹색 필드 배경 asset으로 변경
- 스코어 등록 전 카드와 결과 카드에서 동일한 기본 배경을 사용하도록 정리

### 6. 용병 선택 모달 레이아웃 수정

- CTA 하단 여백 조정 이후 깨졌던 용병 선택 모달 레이아웃 복구
- Figma 기준으로 다음 수치 재반영
  - 문구와 버튼 사이 간격 48px
  - 버튼 하단 여백 24px
- 텍스트 기본 마진 제거로 실제 간격이 어긋나던 문제 수정

## 기술 메모

- 메인 화면 변경 파일:
  - `src/app/components/MainScreen.tsx`
- 용병 모달 변경 파일:
  - `src/app/components/MercenaryModal.tsx`

## 배포 정보

- 브랜치: `chore/supabase-migration-setup`
- 관련 커밋:
  - `7fb4b6b` `Refine main match cards and mercenary modal`
  - `87c8ad5` `Update default match background asset`
- 운영 배포 주소:
  - `https://team-bulgaemi.vercel.app`
