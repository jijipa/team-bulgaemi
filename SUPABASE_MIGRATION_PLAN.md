# Supabase Migration Plan

## 한 줄 요약

지금은 앱이 `구글 시트`를 원본 데이터 장부로 쓰고 있고, 앞으로는 그 장부를 `Supabase`로 옮기는 작업이다.

## 이 문서의 목적

이 문서는 디자인이나 화면 개편 문서가 아니다.

이 문서는:

- 지금 서비스가 데이터를 어떻게 쓰는지
- 왜 Supabase로 옮기려는지
- 무엇을 먼저 정리해야 하는지
- 어떤 순서로 옮기면 안전한지

를 이해하기 쉽게 정리한 계획서다.

## 현재 구조를 쉽게 설명하면

지금 서비스는 이렇게 돌아간다.

- 앱 화면은 사용자가 보는 입력기다
- 실제 기록은 구글 시트에 쌓인다
- 앱은 구글 시트에서 데이터를 읽어와 화면에 보여준다
- 일부 데이터는 브라우저 안에 임시 저장되기도 한다

쉽게 비유하면:

- `구글 시트` = 진짜 장부
- `앱 화면` = 장부를 보기 좋게 쓰는 화면
- `브라우저 임시 저장` = 잠깐 저장해두는 메모장

중요한 전제:

- **원본 데이터는 구글 시트다**
- 이번 마이그레이션은 원본 장부를 구글 시트에서 Supabase로 바꾸는 일이다

## 왜 Supabase로 옮기나

구글 시트는 처음 만들기 쉽고 직접 확인하기도 쉽다.
하지만 서비스가 커질수록 아래 문제가 생기기 쉽다.

- 데이터가 많아질수록 관리가 복잡해진다
- 기능이 늘어날수록 시트 구조가 꼬이기 쉽다
- 수정, 삭제, 연결 관계 관리가 점점 어려워진다
- 이미지, 권한, 사용자 관리, 검색 확장성이 약하다

Supabase로 옮기면 기대할 수 있는 점:

- 데이터 구조가 더 안정적이다
- 앞으로 기능 확장이 쉬워진다
- 기록 간 연결이 더 명확해진다
- 이미지 저장과 관리가 더 자연스러워진다
- 나중에 로그인, 권한, 통계 기능을 붙이기 쉽다

## 이번 작업의 목표

이번 마이그레이션의 목표는 아래와 같다.

- 구글 시트의 데이터를 안전하게 Supabase로 옮긴다
- 앱 화면은 최대한 유지한다
- 사용자는 기존과 비슷한 방식으로 앱을 쓸 수 있게 한다
- 먼저 "안전한 이전"을 하고, 구조 개선은 그 다음에 한다

즉, 첫 번째 목표는 "더 예쁘게 고치기"가 아니라 "안전하게 옮기기"다.

## 옮겨야 하는 데이터

현재 서비스 기준으로 핵심 데이터는 아래와 같다.

- 경기 정보
- 참가 선수 정보
- 득점/도움 정보
- MOM 정보
- 선수 목록
- 경기 이미지 URL

구글 시트 기준으로는 보통 아래 탭들이 해당된다.

- `Matches`
- `Participants`
- `Scores`
- `MOMs`
- `Players`

실제 운영 시트에 탭 이름이 다르면, 그 실제 이름을 기준으로 다시 맞춘다.

## 가장 중요한 원칙

### 1. 원본은 끝까지 구글 시트로 본다

마이그레이션이 완전히 끝나기 전까지는 구글 시트를 기준 데이터로 본다.

이유:

- 문제가 생겼을 때 복구 기준이 필요하다
- 데이터가 누락되거나 어긋났는지 비교할 수 있다

### 2. 한 번에 갈아엎지 않는다

안전하게 가려면 아래 순서가 중요하다.

1. 구조를 정리한다
2. Supabase에 같은 구조를 만든다
3. 데이터를 옮긴다
4. 앱 연결을 바꾼다
5. 충분히 테스트한다
6. 마지막에 구글 시트 의존성을 제거한다

### 3. 처음에는 구조를 최대한 비슷하게 옮긴다

처음부터 데이터를 너무 예쁘게 재설계하면 이사 중에 실수가 난다.

그래서 1차 목표는:

- 구글 시트의 구조를 Supabase에 최대한 비슷하게 옮기는 것

2차 목표는:

- 안정화 후 더 좋은 구조로 정리하는 것

## 작업 단계

## 1단계. 구글 시트 구조 정리

이 단계는 가장 중요하다.

해야 할 일:

- 실제로 쓰는 시트 탭 목록 확정
- 각 탭의 열 이름 정리
- 각 열이 무슨 의미인지 설명 정리
- 어떤 화면이 어떤 탭을 읽고 쓰는지 연결 정리

예시:

- 경기 등록 화면 -> `Matches`
- 참가자 저장 -> `Participants`
- 득점 입력 -> `Scores`
- MOM 저장 -> `MOMs`
- 선수 목록 -> `Players`

이 단계의 결과물:

- "기능 -> 구글 시트 탭" 표
- "탭 -> 열 설명" 표

## 2단계. Supabase 구조 설계

이 단계에서는 구글 시트 탭을 Supabase 테이블로 대응시킨다.

쉽게 생각하면:

- 시트 탭 하나 = Supabase 테이블 하나

예시:

- `Matches` -> `matches`
- `Participants` -> `participants`
- `Scores` -> `scores`
- `MOMs` -> `moms`
- `Players` -> `players`

이 단계에서 정할 것:

- 각 테이블 이름
- 각 칼럼 이름
- 어떤 데이터가 필수인지
- 어떤 데이터끼리 연결되는지

이 단계의 목표는 "아름다운 설계"보다 "안전한 대응 구조"다.

## 3단계. 테스트용 Supabase 만들기

바로 실사용 데이터에 붙지 않고, 먼저 테스트용 공간에서 확인한다.

해야 할 일:

- Supabase 프로젝트 준비
- 테이블 생성
- 이미지 저장용 공간 준비
- 필요한 접근 설정 정리

이 단계에서는 아직 앱을 완전히 바꾸지 않는다.

## 4단계. 구글 시트 데이터 옮기기

이 단계는 실제 이사 작업이다.

해야 할 일:

- 구글 시트 데이터를 추출
- 형식이 다르면 정리
- Supabase에 넣기
- 일부 샘플부터 검증
- 전체 데이터 이관

이 단계에서 꼭 확인해야 할 것:

- 경기 수가 같은가
- 각 경기의 참가자 수가 같은가
- 각 경기의 득점 수가 같은가
- MOM 데이터가 같은가
- 이미지 URL이 빠지지 않았는가

## 5단계. 앱 연결 바꾸기

이제 앱이 구글 시트 대신 Supabase를 보도록 바꾼다.

사용자 입장에서는 ideally 아래처럼 보여야 한다.

- 화면은 거의 그대로다
- 쓰는 방법도 거의 그대로다
- 그런데 저장 위치만 구글 시트에서 Supabase로 바뀐다

이 단계에서 바뀌는 것:

- 경기 목록을 구글 시트가 아니라 Supabase에서 읽음
- 경기 등록을 Supabase에 저장함
- 참가자/득점/MOM도 Supabase에 저장함
- 이미지도 Supabase 기준으로 관리함

## 6단계. 병행 테스트

이 단계에서는 두 장부를 비교한다.

- 구글 시트 결과
- Supabase 결과

비교 포인트:

- 같은 경기 수가 보이는가
- 같은 점수가 보이는가
- 같은 참가자가 보이는가
- 같은 MOM이 보이는가
- 메인 화면 카드가 동일하게 보이는가

이 단계에서 문제가 있으면 Supabase 구조나 매핑을 수정한다.

## 7단계. 최종 전환

모든 기능이 안정적으로 확인되면 마지막 전환을 한다.

마지막으로 하는 일:

- 앱의 기본 원본을 Supabase로 확정
- 구글 시트 의존 코드 제거
- 구글 시트는 보관용 백업으로 남기거나 종료

## 기능별 관점에서 본 이전 순서

복잡한 기능부터 한 번에 옮기지 말고, 아래 순서가 안전하다.

### 우선순위 1

- 선수 목록
- 경기 목록 읽기
- 경기 기본 정보 저장

이유:

- 구조가 단순하고 검증이 쉽다

### 우선순위 2

- 참가자 저장
- MOM 저장

이유:

- 경기와 선수의 연결만 잘 맞으면 된다

### 우선순위 3

- 득점/도움 저장
- 경기 수정

이유:

- 데이터 연결과 복원 로직이 더 복잡하다

### 우선순위 4

- 이미지 업로드

이유:

- 텍스트 데이터보다 검증 포인트가 더 많다

## 이 프로젝트에서 예상되는 주요 리스크

### 리스크 1. 시트 구조와 실제 앱 동작이 완전히 같지 않을 수 있다

증상:

- 앱은 어떤 열을 기대하는데 실제 시트는 다른 이름을 쓰고 있을 수 있다

대응:

- 실제 운영 시트를 기준으로 최종 매핑표를 만든다

### 리스크 2. 일부 데이터가 브라우저 임시 저장에만 남아 있을 수 있다

증상:

- 앱에는 보이는데 시트에는 없는 데이터가 있을 수 있다

대응:

- 배포 전 샘플 기기/브라우저 데이터를 확인한다

### 리스크 3. 득점 데이터 구조가 가장 복잡할 가능성이 높다

증상:

- 경기별 합계, 선수별 기록, 쿼터별 정보가 섞여 있을 수 있다

대응:

- 득점 관련 표를 별도로 만들어 검증한다

### 리스크 4. 이미지 링크가 깨질 수 있다

증상:

- 이전 후 카드 이미지가 안 뜰 수 있다

대응:

- 이미지 URL 규칙을 먼저 정리하고 샘플로 검증한다

## 완료 기준

아래가 만족되면 마이그레이션 완료로 본다.

- 경기 등록이 정상 동작한다
- 경기 목록이 정상 표시된다
- 참가자 저장이 정상 동작한다
- 득점/도움 저장과 수정이 정상 동작한다
- MOM 저장이 정상 동작한다
- 이미지가 정상 표시된다
- 기존 구글 시트 데이터와 결과가 크게 다르지 않다
- 테스트 후 구글 시트 없이도 서비스 운영이 가능하다

## 추천 산출물

마이그레이션 전에 아래 문서를 함께 만드는 것을 권장한다.

- `GOOGLE_SHEETS_TO_SUPABASE_MAP.md`
- `SUPABASE_SCHEMA_OVERVIEW.md`
- `MIGRATION_CHECKLIST.md`

## 바로 다음 액션

다음 순서로 진행하면 된다.

1. 실제 구글 시트 탭 목록 확정
2. 각 탭의 열 이름 정리
3. 기능별로 어떤 탭을 쓰는지 표로 정리
4. 그 표를 기준으로 Supabase 테이블 초안 작성

## 가장 쉬운 요약

지금은 `앱 -> 구글 시트` 구조이고,
앞으로는 `앱 -> Supabase` 구조로 바꾸는 일이다.

핵심은 화면을 바꾸는 것이 아니라,
서비스의 "원본 데이터 장부"를 안전하게 이전하는 것이다.
